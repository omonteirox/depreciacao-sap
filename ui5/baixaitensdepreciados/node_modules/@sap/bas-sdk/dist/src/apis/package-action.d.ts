import { AxiosResponse } from "axios";
export declare function createActionRequest(data: {
    startupServerUrl: string;
    jwt: string;
    form: Record<string, any>;
    enforceSecurityVerification?: boolean;
}): Promise<AxiosResponse>;
